services:
  - type: web
    name: feynman-friction-coach
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
      - key: APP_PASSWORD
        sync: false
      - key: PORT
        value: 8000
