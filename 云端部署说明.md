# 把学习教练部署到云端（手机随时可用）

这套工具原本是在你自己的电脑上运行的。部署到云端后，你可以在任何有网络的地方，用 iPhone、iPad 或电脑的浏览器打开它。

这里以 **Render** 免费平台为例。Render 免费版会在一段时间没人访问后自动休眠，第一次打开会慢几秒，这是正常现象。

## 一、准备

你需要：

1. 一个 GitHub 账号。
2. 一个 Render 账号（可以用 GitHub 直接登录）。
3. 你的 DeepSeek API Key。

## 二、把项目上传到 GitHub

1. 打开 GitHub，新建一个私有仓库，名字随意，例如 `feynman-friction-coach`。
2. 把这个文件夹里的所有文件上传到仓库。
3. 注意：不要上传 `.env`、`.venv`、`build/`、`dist/` 和 `release/` 文件夹。这些已经写在 `.gitignore` 里，通常会被自动忽略。

## 三、在 Render 部署

1. 打开 <https://render.com>，用 GitHub 登录。
2. 点右上角「New」→「Blueprint」。
3. 选择你刚才的 GitHub 仓库。
4. Render 会读取项目里的 `render.yaml` 并自动创建服务。
5. 在环境变量 `DEEPSEEK_API_KEY` 处填入你的密钥。
6. 点「Apply」或「Deploy」。
7. 等待几分钟，部署完成后 Render 会给你一个网址，类似：

```text
https://feynman-friction-coach.onrender.com
```

## 四、在 iPhone 上使用

1. 用 iPhone 的 Safari 打开 Render 给你的网址。
2. 点 Safari 底部「分享」按钮。
3. 选择「添加到主屏幕」。
4. 点「添加」。

以后点主屏幕上的图标，就像打开一个 App。

## 五、数据说明

免费平台的存储空间通常是临时的：如果服务重新部署，书架、总结和会话记录可能会丢失。所以重要的学习总结建议定期复制到本地保存。

如果希望数据永久保存，需要额外接入数据库或持久化磁盘，这一步可以之后再做。

## 其他平台

项目里同时提供了 `Dockerfile`，所以也可以部署到 Railway、Fly.io、VPS 或任何支持 Docker 的平台。部署时只需设置环境变量：

```text
DEEPSEEK_API_KEY=sk-你的密钥
```
